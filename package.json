{
  "name": "meet-ai-scribe-root",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "npm start --prefix backend",
    "heroku-postbuild": "npm install --prefix backend"
  }
}